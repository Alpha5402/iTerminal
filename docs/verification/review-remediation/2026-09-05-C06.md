# C06 Console layout verification

- Baseline revision: `1ceeb84156bfaa7d698505113bb2c42c27031eb0`
- Branch: `codex/remediation-c06-console-layout`
- Scope: responsive/layout and diagnostic presentation only; no protocol, database, or Runtime changes.
- Evidence level: L3 for the isolated real Human Console + official MCP Agent browser path. A disposable PostgreSQL 17 container named `iterminal-c06-postgres` used `127.0.0.1:55439`, database `iterminal_test`, and tmpfs storage; the existing user database/container was not accessed.

The Console now uses an inline, keyboard-operable New Session form. Workspace directory is required and defaults only from the current session's `workspaceRoot`; local labels are explicitly local-only and fall back to the workspace basename. The main strip distinguishes `Workspace` from checkpoint `cwd`, shows a readable input target, and keeps UUID/execution details under Diagnostics. Error banners expose Refresh sessions and Dismiss. The global 1120px minimum was removed; narrow and zoomed layouts can scroll.

## Browser evidence

One worker run reached 8/8; the coordinator's final full run reached 7/8, with the difference documented in the table below. The run produced these local QA artifacts (no user content or secrets). They are ignored by Git and copied to `/private/tmp/iterminal-remediation-dispatch-20260905/C06-artifacts/` for coordinator retention:

- `after-1024x768.png`
- `after-1440x900.png`
- `after-200-percent.png`

The test asserts New Session, a running/READY command composer, and Diagnostics can be scrolled into view and focused after the 200% layout transformation. A base `git archive` was extracted to `/private/tmp/iterminal-c06-before-2z8fA4` without creating a worktree, dependencies were installed offline, and its first browser flow produced `before-1024x768.png`, `before-1440x900.png`, and `before-200-percent.png` in the dispatch artifact directory. `current-1309x1249.png` is a post-change empty-session capture and is not baseline evidence.

| Command                                                                                                                        | Result                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm exec prettier --check apps/console/src/web.tsx apps/console/src/styles.css apps/console/src/browser-shared-path.test.ts` | PASS (write/format followed by targeted check)                                                                                                                                                       |
| `pnpm typecheck`                                                                                                               | PASS                                                                                                                                                                                                 |
| `git diff --check`                                                                                                             | PASS                                                                                                                                                                                                 |
| `pnpm test:m5:browser` with isolated DB                                                                                        | One worker run reached 8/8; coordinator recheck was 7/8, with copy/offline also failing when isolated. Base isolated run fails at the same offline wait (known WebSocket flaky/defect, outside C06). |
| base `1ceeb84` targeted browser comparisons                                                                                    | PASS first flow; `shares cwd...` fails earlier at line 381; `fits...` passes; copy/offline isolated fails at the same offline wait                                                                   |
| C06 target + six other non-offline scenarios                                                                                   | Pass (7 total); offline scenario excluded as the reproducible base WebSocket defect                                                                                                                  |
| 1024x768 / 1440x900 / 200% screenshots                                                                                         | PASS for targeted flow; artifacts above                                                                                                                                                              |
| `pnpm verify`                                                                                                                  | PASS: format, lint, typecheck, 202 tests, 53 verification reports, build                                                                                                                             |

Not covered: persistent cross-client labels and true browser-level zoom emulation beyond the test's CSS zoom representation. The browser flow covers empty required Workspace validation, missing directory, ordinary file (non-directory), and chmod(000) fixtures. On this macOS run chmod(000) produced the generic `RUNTIME_UNAVAILABLE / Runtime RPC request failed` banner (without path leakage), while missing/file inputs produced `Workspace root must resolve to an existing directory`; all Refresh/Dismiss actions were exercised by keyboard. The offline WebSocket wait is a reproducible base defect and is not a C06 acceptance claim. L3 is limited to the C06 target Human Console + MCP path and the seven non-offline scenarios.
