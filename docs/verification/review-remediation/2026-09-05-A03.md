# A03 请求 schema 漂移验证

## Revision 与差异

- 基线 revision：`d53fee73b0a870427b9b14de7d49fd375941e774`
- 工作树：`/private/tmp/iterminal-remediation-a03`
- 分支：`codex/remediation-a03-schema-drift`
- 差异范围：公开 Execute schema 增加可选 `approvalId`（非空、最长 256）；公开 Input schema 增加可选、严格对象 `lineInput`，其 `expectedInputVersion` 为非负整数、`expectedInteractionVersion` 为正整数；新增共享 fixtures 与 schema 契约测试，并由 MCP line-input adapter 测试消费 valid/invalid fixtures。

## 环境

本机 macOS arm64，Node.js/pnpm workspace，使用 Ajv 8.20.0 对公开 JSON schema 做实际编译和验证。适配器的真实约束来源为 Application domain、Runtime RPC Zod、MCP Zod 和 Console Zod；HTTP/MCP 的 actor 由认证上下文注入，body actor 不授予权限。

## 命令与结果

| 命令                                                                                                                                                                                                                         | 结果      | 证据                                                                                            |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------------------------------------------------------------- |
| `pnpm exec vitest run --maxWorkers=1 packages/protocol/src/schemas.test.ts`                                                                                                                                                  | PASS      | 1 file, 10 tests                                                                                |
| `pnpm typecheck`                                                                                                                                                                                                             | PASS      | TypeScript noEmit                                                                               |
| `pnpm exec prettier --check packages/protocol/src/schemas.ts packages/protocol/src/fixtures.ts packages/protocol/src/schemas.test.ts apps/mcp/src/line-input.test.ts docs/verification/review-remediation/2026-09-05-A03.md` | PASS      | all changed files formatted                                                                     |
| `git diff --check`                                                                                                                                                                                                           | PASS      | no whitespace errors                                                                            |
| `pnpm exec vitest run --maxWorkers=1 packages/protocol/src/schemas.test.ts apps/mcp/src/line-input.test.ts apps/console/src/server.test.ts`                                                                                  | PASS/SKIP | 3 files, 21 tests pass；1 environment-dependent MCP test skipped；授权本机 Unix socket 路径通过 |
| `pnpm verify`                                                                                                                                                                                                                | PASS      | 授权本机环境：47 files pass、33 skip；193 tests pass、108 skip；53 reports verified；build pass |

## 覆盖与边界

Ajv fixtures 覆盖 Execute 有/无 approval、Input 有/无线输入前置条件、未知字段、错误类型、非法 generation 与非法 line-input 版本；`additionalProperties: false` 保留。MCP adapter 测试复用共享 line-input fixture 和显式 adapter 映射，验证未知字段、错误版本与非法 generation 的拒绝；Console adapter 测试复用 line-input/Execute fixtures，验证合法字段进入 body 解析、body actor 注入被严格拒绝。认证 actor 仍由 adapter/runtime 上下文提供，body actor 不授予权限。授权本机已完成 Console/MCP 适配器定向运行；未宣称 L2/L3。

证据级别：L1（公开 schema 与真实 adapter 契约测试）；全量门禁通过不提升为 L2/L3 场景结论。
