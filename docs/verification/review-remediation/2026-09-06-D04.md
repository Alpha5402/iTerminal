# D04 remediation verification — 2026-09-06

## Revision and scope

Candidate: the final integration diff and commit(s) following `5c59a49ed034bb3d6e59231a4c3e93f20128d4ea` on `codex/remediation-integration`. The original user checkout was not modified.

## Observed contract

Supervisor signs and atomically replaces private files under the same identities and operations. Fake-clock tests cover repeated renewal, failures, wall-clock jumps, expiry and cleanup. Real short-TTL tests cross repeated replacements with the same Session/generation/Shell PID; a corrupt issuer fixture causes expiry rejection, and restoring it restores authorized requests without PTY replacement.

## Commands and evidence

Evidence level: **L2**, limited to the scenarios above. The exact commands, current pass/fail/skip counts, environment and negative attempts are consolidated in [final verification](2026-09-06-final-verification.md). Relevant tests are the card-specific files listed there plus the required shared-path/integration manifests; the full suite exercises the retained public behavior.

See also the [module ownership map](../../plans/review-remediation/module-responsibilities.md) and [final reconciliation](../../plans/review-remediation/final-reconciliation.md).

## Not proven

The issuer remains local administrator authority. The shared CLI inline grant does not renew. No long-running deployment or OS suspension soak is claimed.
