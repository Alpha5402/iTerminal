# iTerminal 整改交接：Astra 单模型阶段暂停点

更新：2026-09-05，Asia/Shanghai。用户要求暂停开发、整理进度，由新任务继续；本次只整理文档和恢复材料，没有继续修代码、重跑测试、提交或推送。

## 先读这段

- **正式验收基线为 19/36，不是 36/36。** 剩余 17 张卡已有未提交实现，部分定向测试通过，尚未整体验收。
- **唯一包含本阶段完整实现的工作树：`/private/tmp/iterminal-remediation-integration`**。
- 分支：`codex/remediation-integration`；HEAD：`5c59a49ed034bb3d6e59231a4c3e93f20128d4ea`。本阶段代码未提交、未推送；暂停时 34 个 tracked 文件修改、26 个 untracked 文件（不计本交接文档）。
- 原始目录 `/Users/alpha/Documents/ChatGPT/iTerminal` HEAD 为 `138dfb6b8a0d69e31bd41dbe0d5eaca1add7d8a1`，有用户既有改动。**不要在那里直接覆盖源码、reset、checkout 或把其旧 diff 当作当前集成成果。**
- 用户此前要求剩余 17 项由 Astra 独立完成，以测试模型成本/时间；Sol/Luna 调度与 worker 已停。新任务不要根据旧 `DISPATCH.md` 自动恢复并行开发。
- 本文件优先描述当前执行状态；原任务卡继续定义验收条件。旧调度文件中的模型建议、历史启动状态与原始未勾选 checkbox 不代表当前进度。

## 原始目标与规则

原审阅 28 项拆为 36 张卡，定义在 `docs/plans/review-remediation/README.md` 和 01–06 六份卡片文档；最终需要逐项证据对账，正常提交推送到仓库已有 `main`，不强推。用户此前已授权该交付，本次交接没有撤销授权，但验收未完成，不能现在发布未验证实现。

继续前阅读 `AGENTS.md`、`TODO.md`、`docs/TERMINOLOGY.md`、ADR 索引和最新 verification。保持一个 generation 对应一个 PTY/Shell，所有写经 Application Actions，单 ExecuteAction，Input/Control 必须匹配当前 generation/execution，合并 PTY 字节不能伪造 stdout/stderr，未知副作用不重放，PTY 丢失须新 generation。

L0=设计/static；L1=单元/contract；L2=真实本地 PTY/Shell/PG；L3=真实 Console + MCP Agent 路径；L4=故障/压力/安全/跨平台/dogfood。下面通过的测试只是相应场景证据，不能推广为整卡 L3/L4 或长期运行恢复。

## 进度矩阵

已验收基线：A01–A06、B01–B07、C01/C02/C03/C06、D03、F01，共 19 张。本阶段没有新增正式验收卡。

| 卡                      | 已写实现与已有证据                                                                                                                     | 接手后还需完成                                                                    |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| C04 canonical cell 样式 | 新 `TerminalConsoleFrame`、frame RPC、SGR 白名单 renderer；terminal screen/copy/renderer 合计 3 文件 18 tests 通过（L1）               | 浏览器真实颜色/宽字符/隐藏文字/恶意序列验证；整体流回归                           |
| C05 有界历史            | normal-buffer markers、epoch/cursor、128KiB 页预算、history RPC/API、历史 overlay；history 单测 1 通过（L1）                           | 配置上限元数据修复，marker/reflow 边界审查，真实滚动/复制/切 session L3           |
| D01 session 故障隔离    | 继承 Sol diff 后完善 trusted scope、BROKEN 晚到 output 防护；owner-registry + durability 2 文件 21 tests 通过，含真实 PG（L1/L2）      | 重新纳入完整故障门禁和证据；不能把继承工作算成 Astra 从零实现                     |
| D02 局部发现            | DB keyset page、最多 4 路探测、2s deadline、partial/unavailable 状态、Console 分页、MCP page 工具                                      | 和 D07 的新真实 router case 通过（L2），其余 router tests 未全回归；分页/超时边界 |
| D04 凭据续期            | private 文件 provider、single-flight 定时续期/backoff、稳定 issuer；续期单测 3 通过；local-stack 3 tests 通过（L1/L2）                 | 跨过期续期失败、时钟漂移、失败状态可见性、重启路径证据                            |
| D05 Agent 身份          | local 默认兼容、每 Agent 独立私有文件，alpha/beta 真实 MCP 跨续期 actor + 相同 PID 验证                                                | 授权/伪装/重启完整回归；身份区分不是 Session ACL                                  |
| D06 共享 CLI            | 默认 authenticated Unix client，显式 `--standalone`，requestId、多路 wait/control、EOF 不关共享 PTY；真实 CLI/daemon test 1 通过（L2） | 无效 grant、旧协议、服务缺失 cases；当前 CLI 配置只读 inline grant，说明边界      |
| D07 全局审批            | Application pending list、router partial 合并、Console 3s inbox polling + selected history；与 D02 联合 case 通过                      | pending RPC AbortSignal 贯通、inbox 下一页 UI、缓存/recovery 边界、浏览器验收     |
| E01 有界 WS 观察        | composite observation、frame version、ACK 后 row delta、30fps 默认、event single-flight/partial 提示                                   | 无新增专项测试；慢客户端/partial/delta/resync、旧协议降级、断连清理验证           |
| E02 固定 byte ring      | 固定容量循环缓冲，随机参考对照、Unicode/oversized/snapshot 测试；2 文件 5 tests 通过（L1）                                             | 可复现 benchmark 脚本和正式报告；单次微基准不代表产品加速                         |
| E03 FIFO/投影热点       | 仅实现复用 16KiB control scratch buffer                                                                                                | profiling、控制协议定向回归、性能比较和报告尚缺；没有实现 projection coalescing   |
| E04 Application 拆分    | waiters、observation text、retention policy、execution cache 模块；最近 3 文件 30 tests 通过（L1）                                     | 职责文档、完整行为回归，核对拆分是否达到原卡要求                                  |
| E05 Console 拆分        | console-client、stream-controller、diagnostics、history/renderer 模块；typecheck/build 已过                                            | inbox hook 等仍未完成；重构后浏览器未重跑                                         |
| E06 必选集成门禁        | 新 verify-integration 脚本，前置环境检查，JSON 检查每个 required test 均 passed；缺环境负例正确失败                                    | 正向未跑；引用的 `remediation-browser.test.ts` 尚不存在；脚本 lint 检查           |
| F02 Agent 任务基准      | 官方 MCP client + 实际 PG/daemon，10 tests 覆盖 8 类场景；最新 6 过 4 失败                                                             | 先修测试假设，补可靠指标和报告；失败数据不能作为有效性能结论                      |
| F03 多 Agent 授权设计   | 未开始                                                                                                                                 | 仅设计，不实施 ACL；全 RPC 操作矩阵、至少 12 反例、opt-in Session ACL 设计        |
| F04 路线与对账          | 未开始                                                                                                                                 | 原 28 项 ↔ 36 卡 ↔ 实现/证据/限制，更新入口/TODO/checkbox，保留历史证据           |

## 当前失败项：先处理这些

### F02 最新 6 passed / 4 failed

`apps/mcp/src/agent-workflows.test.ts`，最后一次运行约 23:02，13s，执行已结束。

1. long legacy / compact 两项 SHA 不匹配。测试把真实合并 PTY 输出当作纯 payload；回显/prompt 等可能参与。**这是待验证推断。** 先看长度和有限 prefix/suffix，保留完整输出语义，以可验证边界提取 payload 或调整合法 fixture；不要改运行时去丢真实字节，更不要硬改 expected hash 掩盖原因。
2. Python REPL case 5s 超时，可能因 Ctrl-C 只中断、不退出。核实后通过显式 `exit()` 等测试内正常操作收尾，不假设 Ctrl-C 必然杀进程。
3. 非零退出 case 期待 FAILED，实际 `COMPLETED + exitCode: 1`。现有生命周期语义允许命令完成但退出码非零；应修测试断言，不能为了基准改状态机。

已过场景包括丢弃返回后 action lookup/同 key 重查不重复执行、owned shell death -> BROKEN、artifact expiry -> gap。丢弃返回是客户端忽略响应模拟，**不是实际网络断线**，真实 wire-loss 已有其他测试。

暂存原始指标仅用于定位：short legacy/compact 各 3 calls，5632/6484 bytes；long legacy 3 calls/268034 bytes，compact 5 calls/359919 bytes；工具 metadata 约 29936 chars。没有 tokenizer，不能声称 token 成本；compact 当前未表现为更小。失败长输出项不得进入正确性合格的性能对比。

### Console 浏览器与服务端：修复写了，尚未复测

- 最近整份 `browser-shared-path.test.ts`：10 tests，6 过 4 失败，约 78.79s。三个失败与改成按 id 展示 session 后旧 tab 次序不一致有关；已恢复 Console 展示按 createdAt/id 排序。一个 timeline 失败期待 CONSUMED approval，而 pending inbox 替换了 selected history；已恢复 selected history 并合并展示。**以上解释与修复仍需重跑确认。**
- 最近 `server.test.ts`：14 tests，13 过 1 失败，新增 RPC grant operation 排序不符合 canonical 要求；已调整列表，未复测。
- runtime-router `server.test.ts` 的 exact capabilities 列表可能仍缺新值；完整 MCP 工具计数/schema 断言也需核对。
- 最新 typecheck 与 Console build 在约 22:56 通过，之后新增 F02 测试未再 typecheck。本阶段从未完成整套 `pnpm verify`。

## 代码细节与待审风险

- 新 ADR：0077 trusted durability scope、0078 shared CLI + renewable identity、0079 canonical Console observation、0080 partial discovery + approval inbox。均未提交，按实际最终语义校对。
- D01：trusted failure 使用 WeakSet 工厂身份 + session/generation/fencing 精确匹配；不可信/跨 scope 错误仍 owner fail-closed。实际修复过 BROKEN generation 的尾部输出在恢复后打坏新 session 的问题；`#recordOutput` 已忽略 BROKEN。测试最初曾因 patch 未应用跑到旧基线，那次 13 tests 不是此修改的证据。
- D04：公开 mcp.json 仅引用 `ITERM_MCP_CONFIG_FILE`；private MCP 文件里才有 grant，console 使用 private credentials/console.json。续期间隔 80% TTL/最多提前 5min，失败重试 1/2/4…30s，close 等待 pending。测试 TTL 4s，跨两次文件变更仍同 PID。不要打印文件内容或把 credential 文件纳入 Git。
- D02：DB LIMIT 在 join 前、C collation id keyset；历史 BROKEN/CLOSED 不探测 owner。新 page 内部按 id，Console 可见顺序按 createdAt/id。坏 owner 跳过后续探测；写错误仍按既有 `DELIVERY_UNKNOWN`，不能为测试放宽。
- D07：pending cache 只收本 owner 的已受理请求，read 不制造过期 mutation；排除旧 generation。router pending 路径目前没有像 getSession 那样下传 AbortSignal，deadline 后实际请求可能仍运行。全局 inbox 已有 cursor/partial 状态但没有 load-more 入口。
- C05：`retainedLineLimit` 返回值目前硬编码 5000，与测试配置 30 不一致，应按实际配置；marker eviction/reflow/alternate buffer 边界需审。overlay capped 5000 lines，保留软换行复制；session/gen key 防串数据，但 historyOpen 切 tab 行为未正式验收。
- E01：projection 同次读取 cells/frame；Application composite 元数据明确 `atomic:false`，后验 generation/execution 校验两次，否则 RESYNC_REQUIRED。delta 仅使用同 generation/geometry/buffer 且已 ACK baseline，ACK 在实际 render callback 后。旧 runtime capabilities 异常在 WS stream 的兼容路径可能未处理；close 后 eventRead 无取消、stale render callback 与默认 fps 配置都需验证。8MiB WS 上限不代表慢客户端压力验收完成。
- E03：decoder 同步消费/copy scratch，当前无 buffer 跨调用逃逸证据；仍需 control/terminal-response tests。没有已经完成的投影性能结论。
- E04：状态与 settlement 顺序仍由 RuntimeService 管；提取的是协作者，不能让 adapters 接管状态转移。
- E06：必选列表显式列文件、禁止 skip/todo/空 suite，DB 限 `iterminal_test` 且拒绝用户 55432；还没证明正向门禁能通过。

## 环境与安全续跑命令

交接时只读进程检查没有发现本工作树遗留的 vitest/fixture/verify-integration 进程。隔离容器 `iterminal-d01-postgres` 仍运行，绑定 `127.0.0.1:55433 -> 5432`。这是本任务专用测试 DB；用户本地栈的 55432 和真实 MCP/runtime 未操作。后续会清库或 outage 的测试必须串行、只针对该 fixture。

现有 `/private/tmp/iterminal-fixture-tests.py` 从 docker inspect 获取凭据，只注入子进程环境、不打印；副本在交接快照目录 `fixture-tests.py`。它要求现有容器，自动确认/创建专用库。需要外部沙箱权限时只为实际测试所需的 PG/socket/PTY 请求，不把沙箱限制当产品失败。

```sh
cd /private/tmp/iterminal-remediation-integration
git status --short
git rev-parse HEAD
python3 /private/tmp/iterminal-astra-handoff-20260905/fixture-tests.py pnpm exec vitest run --maxWorkers=1 apps/mcp/src/agent-workflows.test.ts
python3 /private/tmp/iterminal-astra-handoff-20260905/fixture-tests.py pnpm exec vitest run --maxWorkers=1 apps/console/src/server.test.ts apps/runtime-router/src/runtime-router.test.ts apps/runtime-router/src/server.test.ts
pnpm build:console
python3 /private/tmp/iterminal-astra-handoff-20260905/fixture-tests.py pnpm exec vitest run --maxWorkers=1 apps/console/src/browser-shared-path.test.ts
```

这些是后续命令，不是本次交接新跑的结果。修复缺失文件与门禁后，再运行：

```sh
python3 /private/tmp/iterminal-astra-handoff-20260905/fixture-tests.py pnpm verify:integration
python3 /private/tmp/iterminal-astra-handoff-20260905/fixture-tests.py pnpm verify:shared-path
python3 /private/tmp/iterminal-astra-handoff-20260905/fixture-tests.py pnpm verify
```

全套 verify 还可能需要 RabbitMQ 等其他 fixture；按 repo 的实际检查前置条件准备，并如实记录 skipped，不从上述 PG helper 推断所有集成环境齐备。每个小修改只跑相关测试；每次 commit/push closure 跑 `pnpm verify` + 当前里程碑真实场景。

保留现有 node_modules。之前 offline install 曾触发 no-TTY purge 问题，不要为清洁环境删除依赖。CLI 新增 `@iterminal/runtime-rpc` workspace 依赖，lockfile 已更新，CLI node_modules 下 workspace symlink 已补并通过 typecheck。不要提交 node_modules。

## 推荐接续顺序

1. 核实该 worktree 和 diff，阅读治理文件/任务卡；不重建全部任务环境，不新开并行 worker。
2. 修 F02 的真实测试假设，复跑近期 Console/router 失败；先得到可信基础回归。
3. 补 C04/C05/D07/E01/E05 的缺口与真实浏览器场景，创建 E06 引用但尚缺的 remediation-browser test，完善强制门禁正向路径。
4. 完成 E02/E03 可复现 benchmark、E04/E05 职责对照、D04/D05/D06 失败/授权路径证据；按卡片验收条件检查遗漏。
5. 完成 F02 报告、F03 设计稿、F04 28→36 项对账；再更新 TODO/checkbox/文档入口，不能先勾选后补证据。
6. 最终 diff review、format/lint/typecheck/full verify + 必选 L2/L3。记录实现/未支持/已验证等级，做 cohesive conventional commits，正常同步推送 main。原主目录有用户改动，避免在那里强制切换分支；选择不覆盖它的 Git 集成交付方式。

## A/B 实验记录边界

Astra 接手约 21:53，最后一批测试约 23:02。该约 70 分钟只是当时实现/测试窗口，不是总任务墙钟或完整成本统计，交接整理也应另记。D01 继承 Sol 约 484 additions / 66 removals + 3 new files；D04 worker 没有实现。不要把剩余卡全部归为 Astra 从零完成。

阶段草稿：`docs/verification/review-remediation/2026-09-05-astra-experiment.md`。没有完成本阶段准确 token 统计、计费或合格端到端比较；历史 coordinator 的 gross/cached token 和提及 verify 的调用数不能冒充实际账单或完整 suite 次数。

原唯一 coordinator `01a07023-3ab2-7f32-9591-78e003896b49` 在停止交接时 idle；重复 coordinator 已停止/归档。仅作定位记录，不要发恢复指令。

## 恢复材料

`/private/tmp/iterminal-astra-handoff-20260905/` 含：

- `tracked.patch`：暂停时相对 HEAD 的 binary diff。
- `untracked.tar.gz`：暂停时 26 个未跟踪源码/文档文件，不含本次新增交接文档。
- `git-status.txt`、`sha256.json`：清单与摘要。
- `fixture-tests.py`：不内嵌凭据的测试 helper 副本。
- `HANDOFF.md`：本交接文档副本。

这是同机器临时快照，不是远端/永久备份。当前 worktree 是首选；仅在它丢失时，在**新的干净目录、精确基线 HEAD** 上先 `git apply --check` 再应用 patch，检查 tar 成员路径后提取 untracked。不要向当前 dirty 工作树重复应用，也不要往用户原始目录套 patch。SHA 摘要覆盖初始快照文件，不含之后生成的 HANDOFF.md。
